#
# Import the SBG Content Catalog
# The SBG Content Catalog is not used but needed for the generic system setup
#

# Language
$lang=en

# Create Content Catalog
UPDATE ContentCatalog;id[unique=true];name[lang=$lang]
;sbgContentCatalog;"TV Channel Content Catalog"
