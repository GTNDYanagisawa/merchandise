#
# Import the CMS Site configuration for the telco store
#
$contentCatalog=telcoContentCatalog
$siteUid=telco

# Load the storefront context root config param
# CMS Site
INSERT_UPDATE CMSSite;uid[unique=true];contentCatalogs(id)[mode=append]
#;$siteUid;$contentCatalog
